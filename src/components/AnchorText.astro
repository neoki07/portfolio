---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'a'> {}
const props = Astro.props;
---

<a class="border-b text-gray-900 border-gray-300 hover:border-gray-500" {...props}><slot /></a>
